%nav.tags

  %header
    %h6= link_to 'Tags', tags_path

  %ul
    = list_of tags do |tag|
      - if tag.diff_status == 1
        %ins= link_to_tag_if_current(tag)
      - elsif tag.diff_status == -1
        %del= link_to_tag_if_current(tag)
      - else
        = link_to_tag_if_current(tag)

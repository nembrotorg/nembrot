%article

  %header
    %hgroup
      %h1= @note.title
      %h2 Note ##{ @note.id }
      %h3= link_to 'Notes', notes_path
      %aside
        %h4#notice= notice

  = bodify(@note.body)

  = render 'tags'

  = render 'versions_list'
